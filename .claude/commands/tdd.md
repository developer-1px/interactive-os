---
description: PRD에서 시나리오를 뽑아 Red-Green-Refactor를 돌리는 전략. /test가 How라면 /tdd는 What.
---

## /tdd — 테스트가 먼저다

### 원칙

> 코드를 쓰기 전에 테스트를 쓴다.
> 테스트가 스펙이고, 통과가 증명이다.
> 실패하는 테스트 없이 코드를 쓰지 않는다.

### 절차

1. **스펙 파악**
   - 구현할 기능의 입력/출력/부수효과를 정의한다.
   - 테스트 레벨을 결정한다 (Unit / Integration / E2E).

2. **PRD 기반 시나리오 커버리지 체크**
   - `docs/1-project/[프로젝트명]/prd.md`를 읽는다. **PRD가 진실의 원천이다.**
   - PRD의 **각 Acceptance Criteria에 대응하는 테스트가 존재하는지** 확인한다.
   - PRD가 없으면 `/prd`를 먼저 실행한다 — 테스트의 근거 없이 테스트를 쓰지 않는다.
   - 테스트는 **수단**(dispatch가 N번 되는가)이 아니라 **목적**(N개가 실제로 붙여지는가)을 검증해야 한다.
   - 누락된 시나리오가 있으면 먼저 테스트를 추가한다.

3. **테스트 먼저 작성 — `/test` 호출**
   - `/test` 워크플로우를 사용하여 적절한 레벨의 테스트를 작성한다.
   - 이 시점에서 구현 코드는 없다. 테스트는 **반드시 실패해야 한다**.
   - 실패하는 테스트 = 스펙의 명시적 선언.

4. **최소 구현**
   - 테스트를 통과시키는 **최소한의 코드**만 작성한다.
   - 과하게 구현하지 않는다. 테스트가 요구하는 것만 구현한다.

5. **통과 확인**
   - 테스트를 실행하여 통과를 확인한다.
   - 통과 = 스펙 충족의 증명.

6. **리팩터 (선택)**
   - 테스트가 통과하는 상태에서 코드를 정리한다.
   - 테스트가 깨지면 리팩터가 잘못된 것이다.

7. **반복**
   - 다음 스펙으로 돌아가 3단계부터 반복한다.

### Red → Green → Refactor

```
🔴 Red    — 실패하는 테스트 작성 (스펙 선언)
🟢 Green  — 최소 구현으로 테스트 통과 (증명)
🔵 Refactor — 테스트 유지하며 코드 정리 (개선)
```

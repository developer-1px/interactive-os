/**
 * useInspectorPersistence — Hook to sync app engine state with inspector
 *
 * Stub re-created after refactoring.
 * Currently a no-op; will be wired when inspector migration completes.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function useInspectorPersistence(_store: any): void {
    // no-op stub
}

/**
 * @deprecated — Use @os/schema instead.
 * This file re-exports from the canonical OS schema location.
 */
export type {
  EffectRecord,
  FocusState as OSFocusState,
  ZoneSnapshot,
} from "@os/schema";
export { INITIAL_OS_STATE as INITIAL_OS_FOCUS_STATE } from "@os/schema";

/**
 * Middleware Types — Re-export from os-new/middleware for backward compatibility.
 */
export type { HistoryEntry, HistoryState } from "@/os-new/middleware/HistoryState";
export type { OSManagedState } from "@/os-new/middleware/OSManagedState";
export type { OSMiddleware } from "@/os-new/middleware/OSMiddleware";

---
description: /test를 이용해 테스트를 먼저 만들고 개발하는 TDD 전략. 테스트가 스펙이고, 통과가 증명이다.
---

## /tdd — 테스트가 먼저다

### 원칙

> 코드를 쓰기 전에 테스트를 쓴다.
> 테스트가 스펙이고, 통과가 증명이다.
> 실패하는 테스트 없이 코드를 쓰지 않는다.

### 절차

1. **스펙 파악**
   - 구현할 기능의 입력/출력/부수효과를 정의한다.
   - 테스트 레벨을 결정한다 (Unit / TestBot / E2E).

2. **테스트 먼저 작성 — `/test` 호출**
   - `/test` 워크플로우를 사용하여 적절한 레벨의 테스트를 작성한다.
   - 이 시점에서 구현 코드는 없다. 테스트는 **반드시 실패해야 한다**.
   - 실패하는 테스트 = 스펙의 명시적 선언.

3. **최소 구현**
   - 테스트를 통과시키는 **최소한의 코드**만 작성한다.
   - 과하게 구현하지 않는다. 테스트가 요구하는 것만 구현한다.

4. **통과 확인**
   - 테스트를 실행하여 통과를 확인한다.
   - 통과 = 스펙 충족의 증명.

5. **리팩터 (선택)**
   - 테스트가 통과하는 상태에서 코드를 정리한다.
   - 테스트가 깨지면 리팩터가 잘못된 것이다.

6. **반복**
   - 다음 스펙으로 돌아가 2단계부터 반복한다.

### Red → Green → Refactor

```
🔴 Red    — 실패하는 테스트 작성 (스펙 선언)
🟢 Green  — 최소 구현으로 테스트 통과 (증명)
🔵 Refactor — 테스트 유지하며 코드 정리 (개선)
```

# QA Report: url-routing

> 일시: 2026-03-12
> 판정: ✅ ALL PASS

| Gate | Result |
|------|--------|
| Code Review | ✅ |
| Contract Compliance | ✅ |
| Simplicity | ✅ |

## Gate 1: Code Review — PASS

- Naming: `pathToHash`, `shouldSyncFromHash` — 프로젝트 동사 사전 준수
- Structure: 순수 함수(`app.ts`) + 부작용 등록(`register.ts`) 분리 깨끗
- Pattern: `os.subscribe()`, `os.dispatch()` from `@os-sdk/os` — 경계 준수
- Dead code: 모든 신규 export가 `register.ts`와 테스트에서 소비됨

## Gate 2: Contract Compliance — PASS

- `os.dispatch` in `register.ts:168`: `.ts` 파일(React 컴포넌트 아님). 기존 미들웨어(L125)와 동일 패턴
- `addEventListener("hashchange")`: 브라우저 API 통합. OS에 동등한 메커니즘 없음. 정당한 예외
- `@os-core/` import in `src/`: 0건

## Gate 3: Simplicity — PASS

- `pathToHash()`: 기존 `parseHashToPath`의 역함수. 제거 불가. 정당
- `shouldSyncFromHash()`: 루프 가드. 2줄이지만 테스트 가능성 + 의도 명시화. 정당
- T3 subscribe 패턴: GO_BACK/GO_FORWARD가 상태 변경 없는 no-op 커맨드라 `defineEffect` 사용 불가. 유일한 메커니즘. 정당
- 총 38줄 추가. 최소 표면적

# KPI — 워크플로 생태계 리팩토링

## 성공 기준

| 지표 | 현재 값 | 목표 값 | 측정 방법 |
|------|---------|---------|----------|
| 중복 워크플로 수 | 1개 (`/next`) | 0개 | `/next` 파일 삭제 확인 |
| 실행 파이프라인 스펙트럼 | 2단계 (`/make`, `/project`) | 3단계 (`/poc`, `/refactor`, `/project`) | 워크플로 파일 존재 확인 |
| 자율 실행 워크플로 | 0개 | 1개 (`/go`) | 워크플로 파일 존재 확인 |
| 라우트 관리 도구 | 0개 | 1개 (`/routes`) | 워크플로 파일 존재 확인 |
| 워크플로 총 수 | 20개 | 22개 (20 - 2 + 4) | `ls .agent/workflows/ | wc -l` |

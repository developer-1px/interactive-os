# 🔍 DnD 전 과정 회고: 파이프라인이 전부 PASS인데 실제로 안 됨

| 항목 | 내용 |
|------|------|
| **원문** | 드래그앤 드롭 만드는 전 과정을 회고 하고 싶어. os gap이 발견되면 지금 하던걸 백로그에 두고 os 개선부터 하는게 맞아 |
| **내(AI)가 추정한 의도** | **경위**: T5 "블록 드래그 정렬"을 /stories→/audit 풀 파이프라인으로 완료했으나, 브라우저에서 전혀 동작하지 않았다. 4개 버그 중 3개가 OS gap. **표면**: 왜 파이프라인이 다 통과했는데 안 됐는지 회고. **의도**: OS 우선 원칙("OS gap 발견 시 feature 중단 → OS 개선 먼저")을 파이프라인에 실체화하고 싶다 |
| 날짜 | 2026-02-26 |
| 상태 | 분석 완료 |

---

## 1. 개요

builder-v2 T5 "블록 드래그 정렬"을 풀 파이프라인(`/stories` → `/spec` → `/red` → `/green` → `/bind` → `/audit`)으로 구현했다. 모든 단계가 PASS였으나, 브라우저에서 드래그 앤 드롭이 전혀 동작하지 않았다.

디버깅으로 4개 버그를 발견했고, **4개 중 3개가 OS gap**이었다.

---

## 2. 타임라인 & 버그 분류

### 파이프라인 결과 vs 실제

| 단계 | 판정 | 실제 |
|------|------|------|
| `/stories` | ✅ US-004 + DT 6행 | — |
| `/spec` | ✅ BDD 4 Scenarios | — |
| `/red` | ✅ 4 FAIL | — |
| `/green` | ✅ 4 PASS | — |
| `/bind` | ✅ 코드 연결 | ❌ 런타임 에러 |
| `/audit` | ✅ 0건 위반 | ❌ 위반 있었음 |
| **브라우저** | — | **❌ 전부 안 됨** |

### 발견된 버그 4건

| # | 버그 | 분류 | 파이프라인이 놓친 이유 |
|---|------|------|---------------------|
| 1 | `data-drag-handle` 속성 누락 | **🔴 OS gap** | DnD 진입점이 DOM convention에 의존. OS가 선언형으로 추상화하지 않음 |
| 2 | `BuilderApp.dispatch is not a function` | **🔴 OS gap** | `onReorder: void` 명령형 시그니처. 다른 콜백(onAction 등)은 선언형(커맨드 리턴) |
| 3 | `e.preventDefault()` → 클릭 차단 | **🔴 OS gap** | MouseListener와 DragListener가 같은 물리 제스처를 다른 API로 처리. Listener 간 격리 안 됨 |
| 4 | `reorderBlocks` 재귀 미지원 | **🟢 앱 로직** | 이건 unit test로 잡을 수 있었음 (실제론 top-level이라 영향 없었음) |

---

## 3. 핵심 발견

### 3-1. OS 추상화 레벨 비교

| 인터랙션 | OS 추상화 수준 | headless test | 브라우저 확인 필요? |
|----------|--------------|--------------|-------------------|
| **키보드** | 완성 | ✅ 충분 | 불필요 |
| **클릭/포커스** | 완성 | ✅ 충분 | 불필요 |
| **드래그** | **미완성** | ❌ 부족 | 필요했음 |

→ **"브라우저에서 직접 확인해야 했다" = OS의 추상화가 부족했다**

### 3-2. OS 우선 원칙의 실체화

> `/go`에 이미 선언됨: "모든 프로젝트의 목적은 앱의 완성이 아니라 OS의 완성이다."

하지만 이 원칙의 **실천 메커니즘**이 없었다:

- `/audit`에서 OS gap을 발견해도, feature를 Done ✅으로 찍고 넘어갔다
- OS gap 발견 시 feature를 **Blocked**로 분류하고 OS 개선 태스크를 먼저 실행하는 게이트가 없었다

### 3-3. "앱 로직 문제" vs "OS gap" 분류 기준

> **앱이 `os.dispatch`를 직접 호출해야 하면 OS gap이다.**
> 선언형(커맨드 리턴)으로 해결할 수 없다면 OS 추상화가 부족한 것.

### 3-4. Listener 아키텍처

현재 MouseListener(`mousedown`/`click`)와 DragListener(`pointer*`)가 **같은 물리 제스처**를 경쟁적으로 처리 → 통합 PointerListener (Gesture Recognizer 패턴)로 전환 필요.

> **원칙: "같은 손가락, 같은 Listener."**

---

## 4. Cynefin 도메인 판정

**🟡 Complicated**

방향은 명확하다 (OS DnD 추상화 개선, Listener 통합). 하지만 구체적 설계(data-drag-handle의 선언형 전환, PointerListener의 edge case 이식)는 분석이 필요하다.

---

## 5. 인식 한계

- 이 분석은 1개 feature(DnD)에서의 관찰에 기반. 다른 OS gap 패턴이 있을 수 있으나 아직 발견되지 않음.
- "앱이 os.dispatch를 직접 호출하면 OS gap" 기준은 DnD에서 도출한 것. 다른 상황에서 이 기준이 충분한지는 추가 검증 필요.

---

## 6. 열린 질문

1. `/audit`에 "OS gap 발견 시 feature Blocked" 게이트를 추가할 것인가?
2. `data-drag-handle`은 `bind()` 옵션으로 선언할 것인가, `<Item>` prop으로 할 것인가?
3. PointerListener 통합의 우선순위는? (현재 DnD는 동작하지만 Listener 충돌 위험은 잠재적)

---

## 다음 액션

| 우선순위 | 액션 | 위치 |
|---------|------|------|
| 1 | `/audit` 워크플로우에 OS gap gate 추가 | `.agent/workflows/audit.md` |
| 2 | OS 우선 원칙 rules.md 명문화 | `.agent/rules.md` |
| 3 | PointerListener 통합 프로젝트 | `docs/5-backlog/2026-0226-unified-pointer-listener.md` |
| 4 | DnD headless test 추상화 (드래그를 커맨드로 시뮬레이션) | 신규 태스크 |

---

## 관련 산출물

| 파일 | 설명 |
|------|------|
| `docs/0-inbox/2026-0226-0800-diagnose-dnd-onreorder.md` | 삽질 일지 (5 Whys) |
| `docs/5-backlog/2026-0226-dnd-pipeline-gap-retrospect.md` | 파이프라인 Gap 회고 |
| `docs/5-backlog/2026-0226-unified-pointer-listener.md` | PointerListener 통합 제안 |

---

> **3줄 요약**
> 1. T5 DnD 풀 파이프라인 PASS → 브라우저 ❌. 4개 버그 중 3개가 OS gap (onReorder void, Listener 충돌, drag-handle convention).
> 2. 핵심 원칙: "브라우저 확인이 필요하면 OS 추상화가 부족한 것." OS gap 발견 시 feature 중단 → OS 개선 먼저.
> 3. 다음: `/audit`에 OS gap gate 추가, rules.md에 OS 우선 원칙 명문화, PointerListener 통합 검토.

# Plan — page-contract-split

> 2026-03-10T13:44

## 영향 범위 전수 조사

### 인프라 파일 (packages/)

| 파일 | 역할 | 변경 필요 |
|------|------|----------|
| `os-sdk/src/app/defineApp/types.ts` | AppPage, AppPageInternal, AppLocatorAssertions 타입 정의 | 삭제 |
| `os-devtool/src/testing/page.ts` (883줄) | createHeadlessPage 구현 | 리팩토링 → createTestEnv |
| `os-devtool/src/testing/index.ts` | re-export | 갱신 |
| `os-devtool/src/testing/types.ts` | Page, Locator, LocatorAssertions 인터페이스 | os 인터페이스 추가 |
| `os-devtool/src/testing/createBrowserPage.ts` | BrowserPage 구현 | AppPage 타입 참조 제거 |
| `os-devtool/src/testing/runScenarios.ts` | vitest bridge | AppPage 참조 제거 |
| `os-devtool/src/testing/simulate.ts` | createHeadlessPage 호출 | 참조 갱신 |
| `os-sdk/src/app/defineApp/index.ts` | AppPage 타입 참조 | 참조 제거 |

### 테스트 파일 (tests/) — OS 전용 메서드 사용 현황

| 메서드 | 호출 횟수 | 변환: `page.X` → `os.X` |
|--------|----------|------------------------|
| `page.focusedItemId` | **90회** | `os.focusedItemId` |
| `page.activeZoneId` | **73회** | `os.activeZoneId` |
| `page.attrs` | **67회** | `os.attrs` |
| `page.kernel` | **13회** | `os.kernel` |
| `page.selection` | **9회** | `os.selection` |
| `page.cleanup` | **8회** | `os.cleanup` |
| `page.html` | **1회** | `os.html` |
| **합계** | **261회** | sed 자동화 가능 |

### 테스트 파일 수: **47개**

## Task Map

| # | Task | Before | After | 크기 | 의존 | 검증 |
|---|------|--------|-------|------|------|------|
| T1 | `OsTestHandle` 인터페이스 정의 | 없음 | `testing/types.ts`에 `OsTestHandle` 추가 (attrs, focusedItemId, selection, activeZoneId, kernel, reset, cleanup, dumpDiagnostics, html) | S | — | tsc 0 |
| T2 | `createTestEnv` 팩토리 작성 | `createHeadlessPage()` → `AppPage` 반환 | `createTestEnv()` → `{ page: Page, os: OsTestHandle }` 반환. 내부는 기존 page.ts 로직 재사용 | M | →T1 | tsc 0 |
| T3 | `AppPage`/`AppPageInternal`/`AppLocatorAssertions` 타입 삭제 | `defineApp/types.ts` L272-385 | 삭제. `createHeadlessPage`는 deprecated wrapper로 유지 (→createTestEnv 위임) | S | →T2 | tsc 0 |
| T4 | 인프라 파일 참조 갱신 | `page.ts`, `index.ts`, `runScenarios.ts`, `createBrowserPage.ts`, `simulate.ts` 등에서 AppPage import | `Page` + `OsTestHandle` import로 교체 | M | →T3 | tsc 0 |
| T5 | 테스트 파일 일괄 마이그레이션 (sed) | 47파일, 261회 `page.{os메서드}` 호출 | `const { page, os } = createTestEnv(...)` + `os.{메서드}` 치환 | M | →T4 | tsc 0 + vitest 774 tests PASS |
| T6 | `createHeadlessPage` deprecated wrapper 제거 + 최종 정리 | deprecated wrapper 잔존 | 완전 제거. export 정리 | S | →T5 | tsc 0 + vitest 774 tests PASS |

## MECE 점검

1. **CE**: T1→T6 순서대로 실행하면 AppPage 완전 삭제, Page+os 분리 달성 ✅
2. **ME**: 중복 없음 ✅
3. **No-op**: Before=After 없음 ✅
4. **L 없음**: 전부 S 또는 M ✅ (T5가 261회 치환이지만 sed 자동화로 M)

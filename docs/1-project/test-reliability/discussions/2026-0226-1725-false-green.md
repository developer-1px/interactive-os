# Discussion: 거짓 GREEN — headless ≠ browser

## Journey

**🧑 사용자**: 이게 dropdown UI가 맞아?

**🤖 AI**: 맞는 지적. Trigger.Portal이 `<dialog showModal()>`로 중앙 모달 렌더. dropdown이 아니라 dialog.

---

**🧑 사용자**: ArrowUp이 왜 테스트가 통과하는데 실제로는 안 돼?

**🤖 AI**: headless 테스트가 `setActiveZone()` 수동 주입. 브라우저 경로를 안 탄다.

---

**🧑 사용자**: e2e는 해결이 아냐. 1000개 만들 거야?

**🤖 AI**: 맞다. 해결은 headless ≡ browser 동치를 구조적으로 보장하는 것. [Reframing: 테스트 수가 아니라 경로 통일]

---

**🧑 사용자**: createPage에 그게 가능하다니까?

**🤖 AI**: `createPage(App, Component)` 발견. 실제 앱 bind 경로 + React 렌더.

---

**🧑 사용자**: dispatch 직접 호출 안 해. 레거시야.

**🤖 AI**: click/press만으로 검증. dispatch = 브라우저 경로 우회.

---

**🧑 사용자**: spec 참고 안 했잖아?

**🤖 AI**: /red Step 1에 spec 게이트 이미 존재. AI가 workflow 파일을 안 읽음.

---

**🧑 사용자**: @spec 링크 강제하면 빠뜨리면 /spec 실행하겠지.

**🤖 AI**: Self-enforcing gate. 별도 코드 불필요.

## Conclusion

| Toulmin | 내용 |
|---------|------|
| **🎯 Claim** | 앱 통합 테스트는 `createPage(App)` + click/press만 사용해야 한다. `createOsPage`는 OS 커널 전용. `@spec` 링크 필수로 spec→red 순서 강제. |
| **📊 Data** | dropdown-dismiss: 8 tests GREEN + audit PASS + doubt PASS → 브라우저에서 Arrow/Escape/overlay 전부 실패 |
| **🔗 Warrant** | `createOsPage`의 `setActiveZone()`은 수동 주입 → 브라우저의 Zone mount 경로와 다르다 → 커널만 검증됨 |
| **📚 Backing** | Testing Trophy (Kent C. Dodds) — integration tests가 가장 높은 ROI |
| **⚖️ Qualifier** | 🟢 Clear |
| **⚡ Rebuttal** | `createPage`도 jsdom/happy-dom 환경이라 native `<dialog>` 동작과 100% 일치하지 않을 수 있음 |
| **❓ Open Gap** | Trigger.Portal 안 Zone의 자동 활성화 메커니즘 (OS 인프라 C2, C3) |

# Task Map — AP-4 Dialog.tsx 삭제 (파일럿)

## Task Map

| # | Task | Before | After | 크기 | 의존 | 검증 | 상태 |
|---|------|--------|-------|------|------|------|------|
| 1 | Dialog.tsx 삭제 + 소비자 ModalPortal 전환 | `packages/os-react/src/6-project/widgets/radix/Dialog.tsx` 259줄. `<Dialog>`, `<Dialog.Trigger>`, `<Dialog.Content>`, `<Dialog.Close>` compound API. 소비자 7개 | Dialog.tsx 삭제. 소비자는 `ModalPortal` + `zone.bind({ triggers })` + `OS_OVERLAY_OPEN/CLOSE` 패턴으로 전환 | M | — | tsc 0 + lint 0 + dialog.layer.test PASS + alertdialog.layer.test PASS + 기존 전체 테스트 regression 없음 | ⬜ |

## 소비자 목록 (전수)

| # | 파일 | Dialog 사용 패턴 |
|---|------|-----------------|
| 1 | `src/pages/layer-showcase/patterns/DialogPattern.tsx` | `<Dialog>` compound |
| 2 | `src/pages/layer-showcase/patterns/AlertDialogPattern.tsx` | `<Dialog>` compound |
| 3 | `src/pages/layer-showcase/patterns/NestedPattern.tsx` | `<Dialog>` compound |
| 4 | `src/command-palette/QuickPick.tsx` | `<Dialog>` compound |
| 5 | `src/apps/todo/widgets/TodoToolbar.tsx` | `<Dialog>` compound |
| 6 | `src/apps/todo/widgets/ListView.tsx` | `<Dialog>` compound |
| 7 | `src/pages/focus-showcase/FocusStackTest.tsx` | `<Dialog>` compound |

## 대체 패턴

```tsx
// Before
<Dialog id="settings">
  <Dialog.Trigger asChild><button>Open</button></Dialog.Trigger>
  <Dialog.Content title="Settings">...</Dialog.Content>
</Dialog>

// After
<button {...triggers.OpenSettings()}>Open</button>
<ModalPortal overlayId="settings" title="Settings">...</ModalPortal>
```

## 참고

- `ModalPortal` 이미 존재: `packages/os-react/src/6-project/widgets/ModalPortal.tsx`
- dialog/alertdialog headless 테스트가 이미 OS primitive만 사용 — Dialog.tsx 무관

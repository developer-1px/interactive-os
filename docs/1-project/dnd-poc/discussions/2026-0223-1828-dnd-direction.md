# Discussion: DnD PoC 방향 결정

## Journey

**🧑 사용자**: 말 나온김에 DnD를 poc만 해두자. 일단 되도록 세팅을 해둬야지. TodoList에서 DnD를 통해서 순서 변경이 되도록 해보자 어때?

**🤖 AI**: Zone 합성점 구조가 실제로 새 capability를 수용하는지 증명할 기회. 핵심 갈림길은 외부 라이브러리(dnd-kit) vs Pointer Events + 커널 커맨드.

---

**🧑 사용자**: 커널 커맨드로 가야해. 그래야 나중에 headless하게 다 관리를 할 수 있지

**🤖 AI**: 맞아. 커널 커맨드 = headless testability + Inspector 관찰성. DnD는 새 Listener → OS_DRAG_START/OVER/END → onReorder 콜백 패턴.

---

방금 완료한 Zone/FocusGroup 분리에서 바로 이어지는 자연스러운 확장.

## Conclusion

- **Why**: Zone 합성점 아키텍처의 확장성을 실증하기 위해
- **Intent**: Pointer Events + 커널 커맨드로 TodoList reorder PoC 구현
- **Warrants**:
  - W1. Zone은 합성점 — headless capability를 끼워넣기 위해 존재
  - W2. FocusGroup headless 패턴이 증명됨 — DnD도 같은 패턴
  - W3. onMoveUp/Down이 이미 존재 — DnD는 같은 연산의 다른 입력 채널
  - W4. PoC = 아키텍처 검증
  - W5. 커널 커맨드 = headless testability + Inspector 관찰성

**한 줄**: DnD는 Zone의 두 번째 headless capability이며, Pointer Events → 커널 커맨드로 구현한다.

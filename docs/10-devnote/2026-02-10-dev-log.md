# 📓 DevLog: 2026-02-10

## 🌡️ Today's Vibe
🔥🏗️💪 **대공사**. Inspector 전체를 뜯어 복원하고, OS Keybinding API까지 연결한 마라톤급 하루.

## 💬 3-Line Summary
- Inspector UI 15개 파일을 git에서 복원하고 `src/inspector/`로 재구조화 — 18개 이상의 TS 에러를 수정
- Inspector의 `Cmd+I` 핫키를 raw `addEventListener` → OS Kernel Command + Keybindings API 플러그인 모델로 전환
- TanStack Router 파일 기반 라우팅 마이그레이션, kernel HMR 안정화, OS-New 폴더 구조 대정리

## 🎞️ Highlights

### 🔄 Inspector 부활기
오늘의 하이라이트는 Inspector 전체 복원. `src/os/app/debug/` 아래 삭제된 15개 UI 파일을 `git show HEAD:<path>`로 하나씩 꺼내서 `src/inspector/`로 재배치했다. import 경로 리매핑이 가장 골치 아팠는데, zsh glob이 `sed` 패턴을 먹어버리는 문제로 3번이나 다시 시도했다 😅

TS4111 (index signature access via dot notation) 에러가 8개나 떴다 — `expandedSections.dom`을 전부 `expandedSections["dom"]`으로 바꿔야 했고, `exactOptionalPropertyTypes`가 `elementId: undefined` 직접 대입을 거부해서 conditional spread `...(elementId != null ? { elementId } : {})`로 우회했다.

### 🔌 OS Keybinding 플러그인 모델
Inspector가 자체 `addEventListener`로 `Cmd+I`를 잡던 방식에서 벗어나, OS의 공개 API를 사용하는 플러그인 모델로 전환했다:

```
Keybindings.register(Meta+I)
→ KeyboardListener → kernel.dispatch(TOGGLE_INSPECTOR)
→ handler 실행 → InspectorStore.toggle()
```

Inspector는 OS에 의존하지만 OS와 강결합되지 않는 깔끔한 구조가 됐다.

### 💥 무한 루프 사건
Inspector 열자마자 "Maximum update depth exceeded"! 원인은 `InspectorRegistry.getPanels()`가 **매 호출마다 `Array.from()` 새 배열**을 반환해서 `useSyncExternalStore`가 매번 "변경됨!"으로 인식 → 무한 재렌더. 캐시된 스냅샷 패턴으로 해결.

### 🏗️ OS-New 대구조 정리
- `1-sensor/` → `1-listeners/`로 통합 (clipboard, focus, history)
- `2-command/` → `3-commands/` 하위로 병합 (navigate, expand)
- `3-store/` → `store/`, `4-effect/` → `middleware/`
- Builder 프리미티브를 `src/apps/builder/`로 분리
- 새로운 `Field.tsx` 컴포넌트 + `useEditing` 훅 추가

## 💎 Discoveries & Insights

- **`useSyncExternalStore` + `Array.from()` = 💣**: `getSnapshot`이 매번 새 참조를 반환하면 무한 루프. 반드시 스냅샷 캐싱 필요.
- **`exactOptionalPropertyTypes`의 영향**: `undefined`를 optional property에 직접 대입 불가 → conditional spread 필수.
- **Kernel Effect → EffectMap 패턴**: 커맨드 핸들러가 `{ effectName: value }` 반환 → `defineEffect`에서 매칭. side-effect 커맨드도 이 패턴으로 통일 가능.
- **Vite 캐시 문제**: alias를 바꾼 뒤에는 `node_modules/.vite` 삭제 + dev server 재시작 필수. 아니면 이전 resolve 결과가 남아서 import 에러.

## 🚀 Next Steps
- [ ] `KernelPanel` generic 타입 이슈 수정 (pre-existing 3 errors)
- [ ] Inspector 패널별 기능 검증 (Element, Registry, State, TestBot)
- [ ] Biome lint 정리 (pre-commit hook 통과하도록)
- [ ] Inspector에서 kernel transaction 렌더링 최적화 (대량 이벤트 시 성능)

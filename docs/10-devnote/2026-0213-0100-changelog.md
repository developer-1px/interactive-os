# 2026-02-13 Changelog

## `ca109e2` — fix: preserve native clipboard when no app override

**문제**: `KeyboardListener`가 `Meta+C/X/V`를 keybinding으로 가로채서 `e.preventDefault()` 호출 → 앱이 clipboard를 오버라이드하지 않은 곳(Docs 등)에서도 네이티브 복사가 죽음.

**수정**:

| 파일 | 변경 |
|---|---|
| `osDefaults.ts` | `Meta+C/X/V` 키바인딩 제거 — ClipboardListener로 위임 |
| `ClipboardListener.tsx` | `canZoneHandle()` 가드 추가 — zone에 콜백 없으면 네이티브 동작 유지 |
| `clipboard.ts` (todo) | `ClipboardItem` sync throw try-catch + 코드 정리 |
| `issue.md` (workflow) | `/issue` 1단계를 `0-issue/` 폴더 + 원문/해석/첫감 포맷으로 변경 |
| `native-clipboard-blocked.md` | 이슈 등록 → closed |

**검증**: tsc ✅ · clipboard-commands.test 5/5 ✅ · 브라우저 Todo ⌘C→⌘V ✅ · Docs 네이티브 복사 ✅

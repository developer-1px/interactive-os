# Plan: trigger-listener-gap Phase 2 — T5-T7

## 변환 명세표

| # | 대상 | Before | After | Cynefin | 의존 | 검증 | 위험 |
|---|------|--------|-------|---------|------|------|------|
| 1 | `FocusItem.tsx:useLayoutEffect(disabled)` L178-185 | useLayoutEffect로 setDisabled (pull) | zone.bind에서 disabled items 선언 (push) | Clear | — | 기존 114 tests 유지 | disabled 사용처 확인 필요 |
| 2 | `Trigger.tsx:handleClick` L126-143 | onClick에서 os.dispatch(overlayOpenCmd) + dispatch(onActivate) 직접 호출 | onClick 제거. PointerListener → resolveClick → OS_ACTIVATE → onActivate 경유 | Complicated | →#1 | Trigger 관련 테스트 전부 PASS | PointerListener의 activeZoneId 조건과 충돌 가능 |
| 3 | `Item.tsx:ExpandTrigger/CheckTrigger` L170-240 | onClick + stopPropagation으로 OS_EXPAND/OS_CHECK dispatch | PointerListener가 data-expand-trigger/data-check-trigger 인식 | Complicated | — | expand/check 테스트 PASS | PointerListener 확장 범위 |

## MECE 점검

1. CE: 1~3 실행하면 "OS 컴포넌트에서 React 이벤트 핸들러 제거" 달성? → ✅
2. ME: 중복? → 없음
3. No-op: → 없음

## 라우팅
승인 후 → `/go` (trigger-listener-gap) — T5 Clear 먼저, T6/T7 Complicated 순차

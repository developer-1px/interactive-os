# BOARD — bdd-tdd-gate

> Light project. 워크플로우 4개 수정.

## 🔴 Now

(없음 — 모든 태스크 완료)

## ✅ Done

- [x] T1. `/tdd` 워크플로우 수정 ✅ — `.feature` → Red → Green 3단계 체인 + 게이트 명시
- [x] T2. `/go` 워크플로우 수정 ✅ — Step 9에 `.feature` + Red 게이트 추가
- [x] T3. `/issue` 워크플로우 수정 ✅ — D5 Feature(재현 .feature) + D6 Red + D7 Green + D8 Verify + D9 Close
- [x] T4. `/test` 워크플로우 수정 ✅ — Level 0 BDD Scenarios (Gherkin) + `features/` 디렉토리 추가

## 💡 Ideas

- 기존 테스트에 대한 소급 `.feature` 작성 (범위 미정)
- `.feature` ↔ `.test.ts` drift 방지 자동화 (린트?)

# Audit — headless-zone-registry (2026-02-27)

## 감사 대상
- `src/os/6-components/base/FocusGroup.tsx` (변경 파일)

## 결과

### 위반 전수 열거

| # | 파일:줄 | 위반 패턴 | 코드 스니펫 | 분류 |
|---|---------|----------|------------|------|
| 1 | FocusGroup.tsx:476 | `querySelector` | `containerRef.current?.querySelector("[data-focus-item]")` | ⚪ 정당한 예외 |

### 분류 상세

**#1 querySelector DOM fallback**: `getItems()`가 없는 레거시 사용처를 위한 fallback. headless 경로(useMemo + getItems)가 우선. `if (_getItems) return;` 가드로 headless 시 실행 안 됨.

### 0건 규칙 추가 검증

1. **OS 프리미티브 전수**: ZoneRegistry.register, OS_ZONE_INIT, OS_FOCUS, useMemo, useLayoutEffect ✅
2. **콜백 시그니처 선언형**: ZoneCallback = `(info) => BaseCommand | BaseCommand[]` ✅
3. **bind() 메소드 존재 확인**: 해당 없음 (OS 내부 코드)
4. **앱 os.dispatch 직접 호출**: 해당 없음 (OS 내부)

## 지표

```
총 위반: 1건
  🔴 LLM 실수: 0건
  🟡 OS 갭: 0건
  ⚪ 정당한 예외: 1건 (querySelector DOM fallback)
재감사: 해당 없음 (🔴 0건)
```

## 결론

✅ 감사 통과. 변경이 OS 내부 아키텍처 개선이며 계약 위반 없음.

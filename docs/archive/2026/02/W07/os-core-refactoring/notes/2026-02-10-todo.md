# TODO — 2026-02-10

> 소스: git log, docs/0-inbox, docs/1-project, docs/2-area, 대화 히스토리

---

## 📋 Recent (완료된 작업)

### Added
- **Kernel meta dispatch** — Transaction에 input event 메타데이터 직접 포함, Inspector에서 단일 트랜잭션으로 확인 가능
- **TanStack Router** — 파일 기반 라우팅으로 전환, docs 서브 경로(`/docs/some/path`) 정상 동작
- **Unified Inspector** — 이벤트 → 커맨드 → 이펙트 흐름을 통합 뷰로 시각화, 요소 선택 락 기능
- **Playwright E2E 환경** — TestBot 테스트 74개를 Playwright 스펙으로 변환, in-browser runner 구축
- **TestBot Cursor 리팩토링** — 명령형 DOM 조작 → React 컴포넌트(CursorOverlay) + Zustand 스토어
- **Kernel 인프라 강화** — HMR 안전, 최대 TypeScript 엄격성, Context-First Curried API, 파일 21→11 통합

### Changed
- **폴더 구조 리팩토링** — `os-new/` 번호 충돌 분석 완료, Legacy Pipeline ↔ Kernel Pipeline 이중 구조 문서화
- **Dead code 정리** — proxy re-export 24개 제거, 60개 미사용 파일 삭제, `ts-morph` 의존 제거

### Fixed
- **Docs 라우팅** — `App.tsx` dead file 제거, TanStack Router `navigate` API로 전환
- **Import 에러** — spec-wrapper plugin 적용 문제 해결, 다수 broken import path 수정
- **Babel 중복 속성** — `data-inspector-loc/line` 중복 경고 해결

---

## 🔴 Now (이번 주)

- [ ] **Legacy Pipeline 폴더 통합** — `1-sensor/`, `2-command/`, `3-store/`, `4-effect/` 를 Kernel Pipeline으로 흡수하여 번호 충돌 해소 ([정리 계획](file:///Users/user/Desktop/interactive-os/docs/1-project/os-core-refactoring/2026-02-10_Legacy_Pipeline_Cleanup_Plan.md))
  - [ ] Phase 1: Dead Code 제거 (`*Command.ts` 래퍼, old KeyboardIntent/Sensor)
  - [ ] Phase 2: `FocusData` → Kernel State 전환
- [ ] **FIELD_* 커맨드 Kernel 등록** — `FIELD_START_EDIT`, `FIELD_COMMIT`, `FIELD_CANCEL`, `FIELD_BLUR`, `FIELD_SYNC` 5개 (Todo/Kanban 앱의 핵심 인터랙션)
- [ ] **Field 컴포넌트 Kernel 기반 재작성** — Zustand `FieldRegistry` + EventBus → Kernel dispatch

---

## 🟡 Next (이번 달)

- [ ] **CommandEngineStore → Kernel 완전 전환** (Phase 3)
  - [ ] `App.tsx`: `defineApplication` → kernel `group({ scope })`
  - [ ] `Trigger.tsx`: old dispatch → kernel dispatch
  - [ ] Apps (todo, kanban): kernel group 기반 커맨드 등록
- [ ] **Legacy Pipeline 최종 삭제** (Phase 4) — `os/features/command/`, `keyboard/pipeline/`, `focus/pipeline/` 디렉토리 제거
- [ ] **COPY/CUT/PASTE/DELETE/UNDO/REDO** Kernel 커맨드 등록
- [ ] **`useFocusRecovery`** Kernel `useComputed` 기반 재구현
- [ ] **os-new 커맨드 단위 테스트** — `3-commands/` 각 커맨드에 순수함수 테스트 추가

---

## 🟢 Later (미래)

- [ ] **Builder* 컴포넌트 처리** — Kernel 전환 또는 `pages/builder/`로 분리 (OS 레이어에서 제거)
- [ ] **Trigger, Label, Root 컴포넌트** Kernel 기반 전환
- [ ] **PersistenceAdapter 구현** — 현재 타입만 존재(`schema/state/PersistenceAdapter.ts`)
- [ ] **`os/` 폴더 완전 삭제** — 모든 consumer가 `os-new/`로 전환된 후
- [ ] **`os-new/` → `os/`로 리네임** — 최종 폴더명 정리
- [ ] **focus-showcase 기반 Playwright 스냅샷 테스트** — CI 통합
- [ ] **spike/ 데모 정리** — 검증 완료된 코드 삭제 또는 `pages/`로 이동

---

## ❗ Blockers

| 블로커 | 설명 | 영향 |
|---|---|---|
| **Builder* 유지 여부 결정** | 6개 Builder 컴포넌트가 NCP 데모 전용 — Kernel 전환 대상인지, 분리해야 하는지 결정 필요 | `os/` 삭제 시점 |
| **Zustand(3-store) 제거 시점** | `6-project/FocusGroup.tsx`가 `3-store/`에 의존 — Zone 완전 대체 시까지 유지 필요 | 이중 상태 관리 |
| **앱 레이어 양방향 의존** | `os/` ↔ `os-new/` 교차 import (~47곳) — 단방향 정리 필요 | 마이그레이션 완료 조건 |
| **마이그레이션 데드라인 미설정** | 점진적 마이그레이션의 최대 리스크는 영원히 완료되지 않는 것 ([Pre-Mortem](file:///Users/user/Desktop/interactive-os/docs/0-inbox/2026-02-10_OS_Migration_Pre-Mortem.md)) | 전체 프로젝트 |

---

> **전체 마이그레이션 진행률**: 약 **65%** (기능 기준)  
> **핵심 다음 단계**: Legacy Pipeline Dead Code 제거 → FIELD 커맨드 Kernel 등록 → CommandEngineStore 제거

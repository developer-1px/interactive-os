# /retrospect — APG Listbox Pattern

## 개발

### Keep
- **Negative test 습관**: 기존 26개 positive에 11개 negative 추가 → 3개 실제 OS 버그 발견
- **W3C APG 원문 참조**: spec 원문 읽고 "MUST NOT" 케이스 도출 → 정확한 테스트

### Problem
- Listbox test가 이미 존재했는데, 처음에 몰랐음 → 중복 작성 시도

### Try
- `find` 먼저 실행해서 기존 테스트 유무 확인 후 작업 시작

## 협업

### Keep
- 사용자가 "Shift가 되면 안되는 거잖아" → 핵심 발견 유도

### Try
- Positive test만 작성하면 절반 → 기본적으로 negative test 포함 습관화

## 워크플로우

### Keep
- `/discussion` → `/go` 파이프라인 자연스럽게 진행

## Action Items

| # | 내용 | 적용 |
|---|------|------|
| 1 | `OS_NAVIGATE` range guard | ✅ `config.select.range` 체크 추가 |
| 2 | `OS_SELECT_ALL` mode guard | ✅ `mode !== "multiple"` 체크 추가 |
| 3 | APG testbot 기본 탑재 | ✅ 6개 scripts, `apgShowcaseScripts` 등록 |

## 증명

| 측정 | 값 |
|------|-----|
| tsc | 0 |
| 신규 tests | +11 negative |
| 총 tests | 551 PASS (OS commands) |
| regression | 0 |
| OS 버그 수정 | 2 (range guard, selectAll guard) |

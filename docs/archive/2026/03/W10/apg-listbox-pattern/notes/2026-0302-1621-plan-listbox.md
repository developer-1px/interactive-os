# /plan — APG Listbox Pattern Headless Tests

> **Goal**: Listbox (single + multi) headless contract test 작성.
> ListboxPattern.tsx는 이미 OS Zone/Item 사용 → OS 변경 불필요 예상.

## 변환 명세표

| # | 대상 | Before | After | Cynefin | 의존 | 검증 | 위험 |
|---|------|--------|-------|---------|------|------|------|
| 1 | `[NEW] listbox.apg.test.ts` | 없음 | Listbox headless contract test: Navigation (vertical, no-loop, Home/End, typeahead), Single-select (followFocus, entry:selected), DOM projection (role, aria-selected, tabIndex) | 🟢 Clear | — | vitest PASS | — |
| 2 | `[NEW] listbox-multi.apg.test.ts` | 없음 | Multi-select: Space toggle (deriveMode→toggle), Shift+Arrow range, Ctrl+A select all, mixed modifier selection | 🟢 Clear | →#1 | vitest PASS | deriveMode auto-resolve 검증 |
| 3 | `ListboxPattern.tsx` (가능성) | 현재 `aria-selected={singleSelection.includes(id)}` 수동 관리 | OS 자동 투영으로 교체 (불필요하면 변경 없음) | 🟢 Clear | — | — | — |

## MECE 점검

1. **CE**: #1+#2 = single + multi 양 모드 headless 커버리지 ✅
2. **ME**: 중복 없음 (#1=single, #2=multi) ✅
3. **No-op**: #3은 조사 후 결정 → 유지

## 라우팅

승인 후 → `/go` (새 프로젝트 `apg-listbox-pattern`) — Red tests 작성 → Green

# Todo App Full Package: Keyboard-First PRD

> **Date**: 2026-02-07  
> **Status**: Draft — ZIFT Coverage Audit  
> **Scope**: 키보드 중심 인터랙션의 완전체 Todo App  

---

## 1. 개요 (Overview)

현재 Reference Todo App은 ZIFT 아키텍처의 벤치마크로서 핵심 기능을 검증했다. 이 PRD는 **"Full Package" — 프로덕션급 키보드 퍼스트 Todo App**을 목표로, 키보드 인터랙션 관점에서 필요한 모든 기능을 정의하고, **ZIFT가 현재 무엇을 커버하고, 무엇이 부족한지**를 명확히 분류한다.

---

## 2. ZIFT 커버리지 현황 분석

### 2.1 ✅ ZIFT가 이미 커버하는 영역 (Implemented)

| 카테고리 | 기능 | ZIFT Primitive | 키바인딩 | 상태 |
| :--- | :--- | :--- | :--- | :--- |
| **네비게이션** | 아이템 간 상하 이동 | Zone (listbox) | `↑` `↓` | ✅ OS 내장 |
| **네비게이션** | 존 간 이동 (Sidebar↔List) | Zone | `Tab` | ✅ OS 내장 |
| **네비게이션** | 포커스 복원 (존 재진입) | Zone `options.navigate.entry: 'restore'` | — | ✅ OS 내장 |
| **네비게이션** | 스크롤 동기화 | FocusSync | — | ✅ OS 내장 |
| **선택** | 단일 선택 (포커스 = 선택) | Zone `options.select.followFocus` | `↑` `↓` Click | ✅ OS 내장 |
| **토글** | 체크박스 토글 | Zone `onToggle` → Trigger `onPress` | `Space` | ✅ 구현됨 |
| **활성화** | 편집 모드 진입 | Zone `onAction` | `Enter` | ✅ 구현됨 |
| **생성** | 새 태스크 추가 (Draft Field) | Field `onSubmit` | `Enter` (Draft 포커스 시) | ✅ 구현됨 |
| **편집** | 인라인 텍스트 수정 | Field `onChange` / `onSubmit` | `Enter` (Save), `Escape` (Cancel) | ✅ 구현됨 |
| **편집** | Draft 실시간 동기화 | Field `onChange` → `SyncDraft` | 타이핑 | ✅ 구현됨 |
| **삭제** | 아이템 삭제 | Zone `onDelete` | `Backspace` / `Delete` | ✅ 구현됨 |
| **삭제** | 포커스 자동 복구 (삭제 후) | OS-Led Recovery | — | ✅ OS 내장 |
| **클립보드** | 복사 | Zone `onCopy` | `⌘C` | ✅ 구현됨 |
| **클립보드** | 잘라내기 | Zone `onCut` | `⌘X` | ✅ 구현됨 |
| **클립보드** | 붙여넣기 (Insert After Focus) | Zone `onPaste` | `⌘V` | ✅ 구현됨 |
| **클립보드** | 복제 | Keymap (Business) | `⌘D` | ✅ 구현됨 |
| **구조** | 아이템 위로 이동 | Keymap (Business) | `⌘↑` | ✅ 구현됨 |
| **구조** | 아이템 아래로 이동 | Keymap (Business) | `⌘↓` | ✅ 구현됨 |
| **뷰** | 리스트/보드 뷰 전환 | Keymap (Global) | `⌘⇧V` | ✅ 구현됨 |
| **사이드바** | 카테고리 선택 | Zone `onAction` / Trigger `onPress` | `Enter` / `Space` / Click | ✅ 구현됨 |
| **사이드바** | 카테고리 순서 변경 | Keymap (Business) | `⌘↑` / `⌘↓` | ✅ 구현됨 |
| **IME** | 한글/일본어 입력 안정성 | Field (Uncontrolled-by-Design) | — | ✅ OS 내장 |
| **Passthrough** | 비관할 영역 네이티브 동작 보장 | Selective Interception | — | ✅ OS 내장 |

### 2.2 ⚠️ ZIFT 프레임워크는 지원하지만 아직 미구현 (Gap — ZIFT Covered)

| 카테고리 | 기능 | ZIFT 메커니즘 | 필요 키바인딩 | 난이도 |
| :--- | :--- | :--- | :--- | :--- |
| **실행취소** | Undo | Zone `onUndo` | `⌘Z` | 🟡 Medium |
| **실행취소** | Redo | Zone `onRedo` | `⌘⇧Z` | 🟡 Medium |
| **다중선택** | 토글 선택 | Zone `select` + `onToggle` | `⌘Click` / `Space` | 🟡 Medium |
| **다중선택** | 범위 선택 | Zone `select` | `⇧Click` / `⇧↑↓` | 🟡 Medium |
| **다중선택** | 전체 선택 | Keymap (Habitual) | `⌘A` | 🟢 Easy |
| **벌크액션** | 다중 삭제 | Zone `onDelete` (multi) | `Backspace` (multi-select 시) | 🟡 Medium |
| **벌크액션** | 다중 복사/이동 | Zone `onCopy`/`onCut` (multi) | `⌘C`/`⌘X` (multi-select 시) | 🟡 Medium |
| **보드뷰** | 2D 공간 네비게이션 | Nested Zones (grid) | `←` `→` (컬럼 간), `↑` `↓` (카드 간) | ✅ 구조 존재 |
| **보드뷰** | 카드 컬럼 간 이동 | Keymap (Business) | `⌘←` / `⌘→` | 🟡 Medium |
| **오토포커스** | Dialog/Modal 초기 포커스 | Zone `autoFocus` | — | ✅ OS 내장 |

### 2.3 🔴 ZIFT 프레임워크 확장이 필요한 영역 (Gap — Needs New Work)

| 카테고리 | 기능 | 필요 변경 | 필요 키바인딩 | 난이도 |
| :--- | :--- | :--- | :--- | :--- |
| **커맨드 팔레트** | Fuzzy Command Search | 새로운 OS Overlay 시스템 | `⌘K` | 🔴 Hard |
| **퀵 캡처** | 글로벌 빠른 입력 | App-외부 글로벌 키 바인딩 | `⌘⇧A` (Anywhere) | 🔴 Hard |
| **드래그 앤 드롭** | 키보드 DnD (이동 모드) | Zone 확장 (Move Mode) | `⌘⇧↑↓` 또는 DnD Mode | 🟡 Medium |
| **컨텍스트 메뉴** | 키보드 Right-Click 메뉴 | Menu Zone + Dialog Physics | `⇧F10` 또는 `ContextMenu` | 🟡 Medium |
| **인라인 날짜** | 날짜 선택기 (Popover) | Popover Zone + Calendar Widget | `⌘D` 또는 인라인 NLP | 🔴 Hard |
| **태그/라벨** | 인라인 태그 추가 | Multi-Value Field 확장 | `#` 트리거 | 🟡 Medium |
| **서브태스크** | 계층적 태스크 (들여쓰기) | Nested Zone + 재귀 Item | `Tab` (들여쓰기), `⇧Tab` (내어쓰기) | 🔴 Hard |
| **검색/필터** | 실시간 검색 + 결과 네비게이션 | Search Field + 결과 Zone 연동 | `⌘F` / `/` | 🟡 Medium |
| **정렬** | 정렬 기준 변경 | Toolbar Trigger | `⌘⇧S` | 🟢 Easy |
| **페이지네이션** | 가상화 스크롤 (대량 데이터) | react-window/virtuoso 통합 | — | 🟡 Medium |

---

## 3. Full Package 키보드 인터랙션 맵 (PRD Spec)

### 3.1 Tier 1: Core Productivity (Must-Have)

> 현재 구현 + Undo/Redo + Multi-Select

| 키 | 컨텍스트 | 액션 | ZIFT 경로 |
| :--- | :--- | :--- | :--- |
| `↑` `↓` | 리스트/보드 | 포커스 이동 | OS 내장 (NAVIGATE) |
| `←` `→` | 보드 | 컬럼 간 이동 | OS 내장 (NAVIGATE) |
| `Tab` | 전체 | 존 간 이동 | OS 내장 (TAB) |
| `Enter` | Draft 포커스 | 태스크 생성 | Field `onSubmit` |
| `Enter` | 아이템 포커스 | 편집 진입 | Zone `onAction` |
| `Enter` | 편집 중 | 저장 | Field `onSubmit` |
| `Escape` | 편집 중 | 취소 | Field `onCancel` |
| `Space` | 아이템 포커스 | 체크 토글 | Zone `onToggle` |
| `Backspace`/`Delete` | 아이템 포커스 | 삭제 | Zone `onDelete` |
| `⌘C` | 아이템 포커스 | 복사 | Zone `onCopy` |
| `⌘X` | 아이템 포커스 | 잘라내기 | Zone `onCut` |
| `⌘V` | 아이템 포커스 | 붙여넣기 | Zone `onPaste` |
| `⌘D` | 아이템 포커스 | 복제 | Keymap (Business) |
| `⌘Z` | 전체 | 실행취소 | Zone `onUndo` 🆕 |
| `⌘⇧Z` | 전체 | 다시실행 | Zone `onRedo` 🆕 |
| `⌘↑` `⌘↓` | 리스트 | 아이템 순서 변경 | Keymap (Business) |
| `⌘A` | 리스트/보드 | 전체 선택 | Keymap (Habitual) 🆕 |

### 3.2 Tier 2: Power User (Should-Have)

> 다중 선택 + 컨텍스트 메뉴 + 검색

| 키 | 컨텍스트 | 액션 | ZIFT 경로 |
| :--- | :--- | :--- | :--- |
| `⇧↑` `⇧↓` | 리스트 | 범위 선택 확장 | Zone `select` mode 🆕 |
| `⌘Click` | 리스트 | 토글 선택 | Zone `select` mode 🆕 |
| `⇧F10` / 우클릭 | 아이템 포커스 | 컨텍스트 메뉴 | Menu Zone 🆕 |
| `⌘F` / `/` | 전체 | 검색 | Search Overlay 🆕 |
| `⌘K` | 전체 | 커맨드 팔레트 | Command Palette 🆕 |
| `⌘⇧V` | 전체 | 뷰 전환 | Keymap (Global) |
| `#` | 편집 중 | 태그 자동완성 | Field 확장 🆕 |

### 3.3 Tier 3: Pro Feature (Nice-to-Have)

> 서브태스크 + 날짜 + 가상화

| 키 | 컨텍스트 | 액션 | ZIFT 경로 |
| :--- | :--- | :--- | :--- |
| `Tab` | 편집 중 (아이템) | 들여쓰기 (서브태스크) | Nested Zone 🆕 |
| `⇧Tab` | 편집 중 (아이템) | 내어쓰기 | Nested Zone 🆕 |
| `⌘⇧D` | 아이템 포커스 | 날짜 선택기 열기 | Popover Zone 🆕 |
| `!p1` ~ `!p4` | Draft 입력 중 | 우선순위 NLP 파싱 | App Logic |
| `⌘⇧S` | 전체 | 정렬 변경 | Toolbar Trigger 🆕 |

---

## 4. ZIFT 커버리지 요약 (Coverage Scorecard)

```
┌─────────────────────────────────────────────────────────────────┐
│  ZIFT Coverage for Full Package Todo                            │
│                                                                 │
│  ████████████████████████████░░░░░░░░░░  ~65%                   │
│                                                                 │
│  ✅ Fully Covered (OS Built-in)         : 12 features           │
│  ✅ Fully Covered (App Implemented)     : 10 features           │
│  ⚠️  ZIFT-Ready, Not Wired             : 10 features           │
│  🔴 New ZIFT Extension Required         : 10 features           │
│                                                                 │
│  Tier 1 Coverage: ~80% (Undo/Redo + MultiSelect 추가만 필요)    │
│  Tier 2 Coverage: ~30% (Menu, Search, CommandPalette 필요)      │
│  Tier 3 Coverage: ~10% (Nested Zone, Popover 신규 개발 필요)    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 결론 및 제안 (Conclusion & Proposal)

### 5.1 핵심 발견

1. **ZIFT의 Tier 1 커버리지는 매우 높다.** 기본 CRUD + 네비게이션 + 클립보드가 이미 Zone Props로 선언적으로 동작한다. 남은 건 **Undo/Redo 배선**과 **Multi-Select Zone 설정**뿐이다.

2. **Habitual vs. Business 분리가 효과적이다.** `onCopy`, `onDelete` 같은 Muscle Memory 액션이 Zone Props로 통합되어, 앱 코드가 극도로 선언적이다. `⌘D`(Duplicate), `⌘↑↓`(Reorder) 같은 Business 로직만 Keymap에 남았다.

3. **확장 필요 영역은 주로 "Overlay" 패턴이다.** Command Palette(`⌘K`), Context Menu(`⇧F10`), Date Picker(`⌘⇧D`)는 모두 **포커스 스택 + Overlay Zone** 패턴을 필요로 한다. 이것은 ZIFT의 Zone 모델 위에 구축 가능하지만, 다이얼로그/팝오버 전용 프리미티브 or 패턴이 필요하다.

4. **서브태스크(Nested Zone)는 가장 큰 아키텍처 도전이다.** 재귀적 Zone 구조에서 `Tab` 키를 들여쓰기 vs. 존 이동으로 분류하는 것은 현재 ZIFT의 Classification Matrix를 확장해야 한다.

### 5.2 추천 로드맵

| Phase | 범위 | 예상 기간 | ZIFT 변경 |
| :--- | :--- | :--- | :--- |
| **Phase 1** | Undo/Redo + Multi-Select | 1~2일 | Zone Props 배선만 (변경 없음) |
| **Phase 2** | Context Menu + Search | 2~3일 | Menu Zone 패턴 정립 |
| **Phase 3** | Command Palette (`⌘K`) | 3~5일 | Overlay Focus Stack 구현 |
| **Phase 4** | Subtasks + Date Picker | 5~7일 | Nested Zone + Popover 확장 |

### 5.3 즉시 실행 가능 (Quick Wins)

- **Undo/Redo**: `historyMiddleware`는 이미 존재. Zone에 `onUndo`/`onRedo` 연결 + Undo/Redo 커맨드 작성만 하면 완료.
- **전체 선택 (`⌘A`)**: Keymap에 추가 + `SELECT_ALL` 커맨드 작성.
- **보드뷰 카드 이동 (`⌘←` `⌘→`)**: Keymap에 `MOVE_CARD_LEFT`/`MOVE_CARD_RIGHT` 추가.

---

*Generated by Antigravity Agent — 2026-02-07T10:46+09:00*

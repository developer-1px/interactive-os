# TODO — 2026-02-13

## 📋 Recent (완료된 작업)

### Added
- **`createTrigger` API** — v3 headless triggers 구현. 마우스 이벤트 디스패치를 OS primitive에서 분리 (`813994d`)
- **`resolveFallback` API** — Mac 키보드 레이아웃 fallback 미들웨어 (`a082bb3`)
- **UnifiedInspector** — Event Stream + Store State 단일 패널 통합
- **defineApp v3 Usage Guide** — `defineApp + createWidget` 패턴 문서화 완료
- **createModule Retrospective** — todoSlice → TodoModule 마이그레이션 회고 (import -32%, 줄 수 -16%)
- **Headless UI Resource** — 패턴 역사·설계 철학·라이브러리 비교 리소스 문서

### Changed
- **키보드 이벤트 아키텍처** — `getCanonicalKey` 정규화 제거, two-pass fallback 방식으로 전환 (`a082bb3`)
- **`os-new/core/` 삭제** — `FocusData` → `kernel/ZoneRegistry` 마이그레이션 (`a3c6bdf`)
- **Legacy middleware/pipeline 완전 제거** — `CommandEngineStore` 삭제 (`387d47f`, `8d9f6fb`)
- **`store/`, `lib/` 제거** — 소비자에 colocation (`7925913`, `ff1260f`)
- **listeners 네이밍 통일** — `keymaps/` 추출 (`17bb393`)
- **docs-viewer 리디자인** — 가독성 우선 UI + 7가지 객관적 검증 규칙 적용
- **PARA inbox 처리** — `command-palette` 아카이브 (`c683cee`)
- **프로젝트 폴더 표준화** — WHY → WHAT → HOW → IF 번호 순서 규칙 확립

### Fixed
- **Native clipboard 보존** — `KeyboardListener`가 앱 override 없을 때 `preventDefault` 안 하도록 수정 (`ca109e2`)
- **TestBot 12/12 PASS** — Playwright shim 패리티 달성 (`89cc142`)
- **TSC 50+ 타입 에러 해결** — kernel + Todo app (`1ce003f`)
- **Builder overlay 복구** — `FocusDebugOverlay`를 kernel state 기반으로 포팅
- **Todo 붙여넣기 포커스 버그** — `FOCUS_ID` effect 문제 진단 (수정 진행 중)

### Removed
- Spike demos 및 라우트 (`03eb1ae`)
- `defineAppCommand` stub (`f2998f3`)
- Legacy `StateMonitor`, `DataStateViewer` 탭

---

## 🔴 Now (이번 주)

- [ ] **`remapping` 아카이브** — 완료 확인됨 (`✅ 구현 완료`), `docs/4-archive/2026/`로 이동
- [ ] **`focus-showcase`, `runner-architecture` 아카이브 처리** — 비활성(Stale), 전제 소멸
- [ ] **Todo 붙여넣기 포커스 버그 수정** — `PasteTodo` 후 `FOCUS_ID` effect가 DOM 포커스를 못 잡는 문제
- [ ] **Todo E2E 나머지 3건** — Meta+Arrow 리매핑 충돌 2건 + Sidebar nav 1건
- [ ] **`clipboard.ts` 이펙트 분리** — `navigator.clipboard` 직접 호출 제거, AppState.clipboard 이동
- [ ] **UnifiedInspector 브라우저 확인** — UI/UX 실제 동작 검증

---

## 🟡 Next (이번 달)

- [ ] **create-module 프로젝트 본격 착수** — PROPOSAL 리뷰 → `/divide` → `createModule.ts` 구현 → TodoModule 정의
- [ ] **focus-recovery 하이브리드 전략 구현** — FocusSync Safety Net + `resolveRecovery` 연결 + 커널 기반 재구현
- [ ] **os-core-refactoring 잔여** — `FIELD_*` 커맨드 등록, Field 컴포넌트 재작성, COPY/CUT/PASTE Kernel 등록
- [ ] **Inspector Shell 일반화** — 하드코딩 탭 → InspectorRegistry 패턴
- [ ] **Todo v2 → v3 위젯 교체** — defineApp v3 검증 완료, 기존 widgets 삭제 후 v3로 전환
- [ ] **Correlation ID** — OS 커맨드 ↔ Kernel Transaction 매핑

---

## 🟢 Later (미래)

- [ ] **Builder* 처리 방향 결정** — Trigger/Label/Root 전환, 유지 여부
- [ ] **PersistenceAdapter** 구현
- [ ] **`os/` → `os-new/` 최종 리네임** — 교차 import ~47곳 해결
- [ ] **Zustand 완전 제거**
- [ ] **카테고리 CRUD 신규 구현** (Todo app)
- [ ] **Inspector Time Travel, 검색/필터** 추가
- [ ] **다른 앱(Builder, Kanban 등) createModule/defineApp 적용**

---

## ❗ Blockers

| 항목 | 설명 |
|------|------|
| **Builder* 유지 여부** | os-core-refactoring 진행에 영향. 삭제 vs 전환 결정 필요 |
| **module.ts 500줄 관리** | 커맨드 분리 re-export vs 단일 파일 유지 결정 |
| **`Widget.Zone` FOCUS 매핑** | `{ id: OS.FOCUS }` 고정 주입 — `{ focusId: OS.FOCUS }` 등 다른 키에 대한 범용 매핑 미결 |
| **Todo 붙여넣기 포커스** | 근본 원인 진단됨, 수정 미완료 |

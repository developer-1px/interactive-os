# Product Vision — TestBot

> LLM이 만든 테스트를 인간이 시각적으로 검증하는 도구.

## Vision

**LLM이 작성한 vitest 테스트 코드를 브라우저에서 시각적으로 재생하여, 인간이 "이 테스트가 진짜 내가 원한 것을 테스트하는가?"를 눈으로 확인할 수 있게 하는 도구.**

코드만으로는 LLM이 뭘 만들었는지 따라가기 어렵다. TestBot은 테스트의 매 단계를 커서 이동, 클릭 하이라이트, PASS/FAIL 스탬프로 시각화하여 인간의 검증 비용을 0에 가깝게 만든다.

## Target Group

**AI와 협업하는 개발자 (우리 자신).**
- LLM에게 코드와 테스트를 맡기되, 결과물을 신뢰하고 싶은 사람
- 테스트 코드를 한 줄씩 읽는 것보다, 실행 과정을 보고 판단하는 것이 빠른 사람
- 인터랙션(포커스, 네비게이션, 셀렉션) 테스트는 특히 코드만으로 추적이 어려움

## Needs

1. **눈으로 확인** — "ArrowDown을 누르면 포커스가 b로 간다"를 글자가 아니라 화면으로 본다
2. **테스트를 위한 별도 코드 0** — vitest 테스트 파일이 유일한 원본. TestBot용 코드를 따로 쓰지 않는다
3. **LLM 개발 흐름에 무개입** — LLM은 vitest만 쓴다. TestBot은 인간 전용
4. **실제 DOM 검증** — headless에서 놓치는 "렌더링 갭"을 실제 DOM에서 교차 검증
5. **State Injection** — 앱을 특정 상태로 강제 설정하여 테스트 전제 조건을 즉시 구성

## Product

### 핵심 기능

| 기능 | 설명 |
|------|------|
| **Visual Playback** | vitest 테스트를 브라우저에서 재생. 커서 애니메이션, 클릭 하이라이트, 타이핑 시각화 |
| **PASS/FAIL Stamps** | 각 assertion의 결과를 DOM 위에 스탬프로 표시 |
| **DOM Cross-Verification** | 커널 상태뿐 아니라 실제 DOM 속성(aria-*, tabIndex, data-focused)을 교차 검증 |
| **State Injection** | `kernel.setState(fixture)`로 OS+앱 상태를 한 번에 주입, 별도 mock helper 불필요 |
| **Suite Browser** | Inspector 내 탭에서 테스트 목록 탐색, 개별/전체 실행 |

### 아키텍처 원칙

| 원칙 | 설명 |
|------|------|
| **vitest 코드가 유일한 원본** | TestBot을 위한 별도 테스트 코드 없음. vitest shim이 브라우저에서 실행 |
| **OS 시그널 기반** | pressKey/click → OS 파이프라인 관통 (Listener → resolve → dispatch → state → render → DOM) |
| **Event 시작점** | dispatch가 아니라 DOM Event(KeyboardEvent, MouseEvent)로 시작하여 전 구간 커버 |
| **기존 자산 유지** | Playwright TestBot(v1)은 폐기하지 않음. E2E는 별도 유지 |

### 테스팅 피라미드에서의 위치

```
  🔺 Playwright E2E      — 블랙박스, 느림, 별도 프로세스
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🔶 TestBot v2           — 같은 vitest 코드, 브라우저, 시각화 + DOM 검증
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🔷 Vitest headless      — 같은 코드, 빠름, 커널 상태만
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ⬜ Unit tests           — 순수 함수
```

## Business Goals

### 1차: AI-Human 협업의 신뢰 인프라

> "에이전트의 시행착오 비용을 줄인다" (rules.md Goal #1)

LLM이 테스트를 작성하면 vitest가 PASS를 보고한다. 하지만 인간은 "PASS가 진짜 의미 있는 PASS인지"를 모른다. TestBot은 이 신뢰 갭을 메운다:

- LLM이 놓친 케이스 → 시각적으로 즉시 발견
- 잘못된 assertion → 화면과 코드의 불일치로 드러남
- 올바른 테스트 → 인간이 확신을 가지고 다음 단계로 진행

### 2차: Interactive OS의 자기 증명

> "이 OS 위에서 이 OS를 테스트한다" (rules.md 검증 #2)

TestBot 자체가 Interactive OS 위에서 동작하는 앱이다. OS의 Focus, Navigation, Selection 인프라를 써서 OS의 Focus, Navigation, Selection을 검증한다.

## Non-Goals

- ❌ LLM의 개발 흐름에 개입 (LLM은 vitest만 쓴다)
- ❌ Playwright E2E 대체 (E2E는 별도 유지)
- ❌ TestBot 전용 테스트 코드 작성
- ❌ CI/CD 자동화 러너 (CI는 vitest + Playwright가 담당)
- ❌ 테스트 작성 도구 / 레코더

## Now / Next / Later

### 🔴 Now — 핵심 재생 엔진

- vitest shim으로 `.test.ts` 파일을 브라우저에서 실행
- pressKey/click 시각화 (커서 애니메이션, 클릭 하이라이트)
- PASS/FAIL 스탬프 오버레이
- State Injection (`kernel.setState`)

### 🟡 Next — DOM 교차 검증

- attrs() 결과와 실제 DOM 비교
- 불일치 시 diff 표시
- 앱별 fixture 프리셋 (Todo 3-task, Builder landing 등)

### 🔵 Later — 고급 시각화

- 테스트 실행 타임라인 (step-by-step 되감기)
- 파이프라인 관통 경로 시각화 (Event → Listener → resolve → command → state → DOM)
- 영상 녹화 / GIF 내보내기

---

## Discussion History

이 비전은 여러 차례 논의를 거쳐 확정되었다:

- **2026-02-10**: TestBot v1 아키텍처 리뷰. Playwright Shim 방식의 한계 발견. Visual Verification이 핵심 가치임을 확인.
- **2026-02-12~13**: Shim parity 달성 (Todo 12/12 PASS). Phase 1~4 제안. 이후 프로젝트 보류→아카이브.
- **2026-02-21**: 리뉴얼 논의. 핵심 전환점 3가지:
  1. "Playwright 흉내내기"에서 "OS 시그널 기반"으로 전환
  2. "테스트 러너"에서 "인간의 시각적 검증 도구"로 목적 재정의
  3. State Injection으로 setup 문제 해결

---

_Format: [Product Vision Board](https://www.romanpichler.com/tools/product-vision-board/) (Roman Pichler) + [Now/Next/Later Roadmap](https://www.prodpad.com/blog/invented-now-next-later-roadmap/) (Janna Bastow)_

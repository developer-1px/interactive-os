# any 제거 전략 Discussion

> 2026-02-18 15:20 ~ 15:22

## Journey

**🧑 사용자**: any 제거를 조금씩 해보고 싶은데 왜 이렇게 any가 점점 늘고 있을까?

**🤖 AI**: 현황 분석 — 전체 105개(routeTree.gen.ts 제외)를 4개 카테고리로 분류:
1. v3 호환 레이어 (`defineApp.widget.ts` 14개 + `defineApp.ts` 13개 = 27개) — 최대 오염원
2. 미완성 Payload 정의 (`OSCommandPayload.ts` 8개) — lazy typing
3. React 타입 한계 (~12개) — 구조적
4. 외부 라이브러리 경계 (~22개) — TanStack Router

v3 widget 패턴이 아직 살아있는지가 핵심 질문.

---

**🧑 사용자**: 마이그레이션은 완료가 되었을텐데 아니면 완성이 안되었다면 완성을 시키는게 우선이 되어야 할것 같아

**🤖 AI**: 검증 결과 — `createWidget` 호출 0건. Todo, Builder 모두 v5 native 완료. 죽은 코드가 any 27%를 점유 중. Clear 판정: 삭제 + payload 정리로 34% 즉시 제거 가능.

---

**🧑 사용자**: 좋아 닫고 진행해

> 한 줄 요약: v3 호환 코드는 이미 죽었다 — 삭제가 곧 any 제거의 최대 레버리지.

---

## Conclusion

### Why
any가 점점 늘어나는 이유는 코드를 수정하지 않는 것이 아니라, "나중에 고치겠다"는 호환 레이어와 lazy typing이 쌓여가기 때문.

### Intent
any 확산의 구조적 원인을 파악하여, 한 줄씩 고치는 게 아니라 근본 메커니즘을 차단하고 싶다.

### Warrants
- **W1.** any의 최대 원천은 v3 호환 레이어(widget) — 제네릭 유실 구조 (27개, 27%)
- **W2.** 미구현 커맨드의 lazy typing이 any를 누적시킨다 (8개)
- **W3.** React/Router 외부 라이브러리 경계의 any는 구조적 한계
- **W4.** routeTree.gen.ts는 자동 생성 — 통제 불가
- **W5.** `createWidget`의 소비자는 0개. Todo, Builder 모두 v5 native 완료. 죽은 코드가 any 27% 점유.

### Action Plan (Clear)
1. `defineApp.widget.ts` 삭제 + `defineApp.ts`에서 `createWidget` 제거 → any 28개 즉사
2. `OSCommandPayload.ts` 미구현 커맨드 payload → `undefined` 또는 `void` → any 8개 제거
3. 나머지 React/Router 경계 any는 개별 파일 단위로 점진적 개선

> 한 줄 요약: 죽은 v3 호환 코드 삭제가 any 34% 즉시 제거의 열쇠이며, 이것이 타입 건전성 회복의 첫 번째 레버리지다.
